import React from 'react';

const Header = () => {
    return (
        <header className="jumbotron mt-4 mb-0 text-center">
            <h1>PINGR</h1>
        </header>
    );
};

export default Header;